<%= form_for(@product_price) do |f| %>
  <% if @product_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_price.errors.count, "error") %> prohibited this product_price from being saved:</h2>

      <ul>
      <% @product_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <span class="form-label"><%= f.label :product_id %></span>
    <span class="form-field"><%= f.collection_select :product_id, @products, :id, :name %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :start_at %></span>
    <span class="form-field"><%= f.datetime_select :start_at, :order => [:day, :month, :year] %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :finish_at %></span>
    <span class="form-field"><%= f.datetime_select :finish_at, :order => [:day, :month, :year] %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :currency_id %></span>
    <span class="form-field"><%= f.collection_select :currency_id, @currencies, :id, :iso_code %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :price %></span>
    <span class="form-field"><%= f.text_field :price %></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
