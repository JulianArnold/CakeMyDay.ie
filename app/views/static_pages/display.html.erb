<% if @page %>
  <h1><%= @page.main_heading %></h1>
  <p><%= sanitize @page.main_body.gsub("\r","<br/>")%>
<% else %>
  <h1>Sorry, we couldn't find what you requested.</h1>
  <p>Return to our <%= link_to "home page", root_url %>.</p>
<% end %>