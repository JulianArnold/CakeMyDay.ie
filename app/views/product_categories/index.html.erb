<h1>Listing product_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>One choice only</th>
    <th>Running order</th>
    <th>Options list</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_categories.each do |product_category| %>
  <tr>
    <td><%= product_category.name %></td>
    <td><%= product_category.description %></td>
    <td><%= product_category.one_choice_only %></td>
    <td><%= product_category.running_order %></td>
    <td><%= product_category.options_list_id %></td>
    <td><%= link_to 'Show', product_category %></td>
    <td><%= link_to 'Edit', edit_product_category_path(product_category) %></td>
    <td><%= link_to 'Destroy', product_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product category', new_product_category_path %>
