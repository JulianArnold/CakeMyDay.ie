<%= form_for(@shopping_cart_item) do |f| %>
  <% if @shopping_cart_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shopping_cart_item.errors.count, "error") %> prohibited this shopping_cart_item from being saved:</h2>

      <ul>
      <% @shopping_cart_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <span class="form-label"><%= f.label :cake_id %></span>
    <span class="form-field"><%= f.number_field :cake_id %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :product_id %></span>
    <span class="form-field"><%= f.number_field :product_id %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :quantity %></span>
    <span class="form-field"><%= f.number_field :quantity %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :product_price_id %></span>
    <span class="form-field"><%= f.number_field :product_price_id %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :product_options_list_choice %></span>
    <span class="form-field"><%= f.text_field :product_options_list_choice %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :global_options_list_choice %></span>
    <span class="form-field"><%= f.text_field :global_options_list_choice %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :user_description %></span>
    <span class="form-field"><%= f.text_field :user_description %></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
