<%= form_for(@options_list) do |f| %>
  <% if @options_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@options_list.errors.count, "error") %> prohibited this options_list from being saved:</h2>

      <ul>
      <% @options_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <span class="form-label"><%= f.label :name %></span>
    <span class="form-field"><%= f.text_field :name %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :visible_label %></span>
    <span class="form-field"><%= f.text_field :visible_label %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :options %></span>
    <span class="form-field"><%= f.text_area :options, :size => "50x5" %></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
