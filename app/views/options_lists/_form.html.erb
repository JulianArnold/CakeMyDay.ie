<%= form_for(@options_list) do |f| %>
  <% if @options_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@options_list.errors.count, "error") %> prohibited this options_list from being saved:</h2>

      <ul>
      <% @options_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :visible_label %><br />
    <%= f.text_field :visible_label %>
  </div>
  <div class="field">
    <%= f.label :options %><br />
    <%= f.text_area :options %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
