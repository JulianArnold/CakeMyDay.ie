<%= form_for(@customer) do |f| %>
  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% @customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <span class="form-label"><%= f.label :postal_address %></span>
    <span class="form-field"><%= f.text_area :postal_address, :size => "30x5" %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :contact_phone_number %></span>
    <span class="form-field"><%= f.text_field :contact_phone_number %></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
