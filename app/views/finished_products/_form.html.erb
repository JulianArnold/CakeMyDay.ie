<%= form_for(@finished_product) do |f| %>
  <% if @finished_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finished_product.errors.count, "error") %> prohibited this finished_product from being saved:</h2>

      <ul>
      <% @finished_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <span class="form-label"><%= f.label :product_name %></span>
    <span class="form-field"><%= f.text_field :product_name %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :description %></span>
    <span class="form-field"><%= f.text_field :description %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :typical_price %></span>
    <span class="form-field"><%= f.text_field :typical_price %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :special_occasion_id %></span>
    <span class="form-field"><%= f.collection_select :special_occasion_id, @special_occasions, :id, :name %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :running_order %></span>
    <span class="form-field"><%= f.number_field :running_order %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :available_for_purchase %></span>
    <span class="form-field"><%= f.check_box :available_for_purchase %></span>
  </div>
  <div class="field">
    <span class="form-label"><%= f.label :visible %></span>
    <span class="form-field"><%= f.check_box :visible %></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
